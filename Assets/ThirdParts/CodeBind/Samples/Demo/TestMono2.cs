using CodeBind;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CodeBindName("TestMono2")]
[MonoCodeBind('_')]
public partial class TestMono2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
