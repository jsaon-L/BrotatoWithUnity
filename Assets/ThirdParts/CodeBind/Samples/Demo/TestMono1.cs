using CodeBind;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[MonoCodeBind]
public partial class TestMono1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
